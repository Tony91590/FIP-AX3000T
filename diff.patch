--- a/uboot-mtk-20250711/failsafe/embedded/fsdata-gl/main.js
+++ b/uboot-mtk-20250711/failsafe/embedded/fsdata-gl/main.js
@@ -69,20 +69,12 @@ function getmtdlayoutlist() {
 }
 
 function getversion() {
-    var versionElem = document.getElementById("version");
     ajax({
         url: '/version',
-        done: function (version) {
-            if (!versionElem) return;
-            versionElem.innerHTML = version + ' 💡Yuzhii';
+        done: function(version) {
+            document.getElementById('version').innerHTML = version
         }
     })
-    if (versionElem) {
-        var projectInfo = document.createElement("div");
-        projectInfo.id = "project-info";
-        projectInfo.innerHTML = 'You can find more infomation about this project: <a href="https://github.com/Yuzhii0718/bl-mt798x-dhcpd" target="_blank">Github</a>';
-        versionElem.parentNode.insertBefore(projectInfo, versionElem.nextSibling);
-    }
 }
 
 function upload(name) {
--- a/atf-20240117-bacca82a8/configs/mt7986_redmi_ax6000_defconfig
+++ b/atf-20240117-bacca82a8/configs/mt7986_redmi_ax6000_defconfig
@@ -1,3 +1,6 @@
 _PLAT_MT7986=y
 _DRAM_DDR4=y
 _LOG_LEVEL_INFO=y
+_NAND_SKIP_BAD=y
+_BOOT_DEVICE_SPIM_NAND=y
+_NAND_TYPE_2K_64=y
--- a/uboot-mtk-20250711/configs-fit/mt7986_redmi_ax6000_defconfig
+++ b/uboot-mtk-20250711/configs-fit/mt7986_redmi_ax6000_defconfig
@@ -48,12 +48,13 @@ CONFIG_CMD_PING=y
 CONFIG_CMD_SMC=y
 CONFIG_CMD_MTDPARTS=y
 CONFIG_MTDIDS_DEFAULT="spi-nand0=spi-nand0"
-CONFIG_MTDPARTS_DEFAULT="spi-nand0:1024k(bl2),256k(Nvram),256k(Bdata),2048k(factory),2048k(fip),256k(crash),256k(crash_log),-(ubi)"
+CONFIG_MTDPARTS_DEFAULT="spi-nand0:1024k(bl2),256k(Nvram),256k(Bdata),2048k(factory),2048k(fip),-(ubi)"
 CONFIG_CMD_GL_BTN=y
 CONFIG_OF_EMBED=y
 CONFIG_ENV_OVERWRITE=y
 CONFIG_ENV_VARS_UBOOT_RUNTIME_CONFIG=y
 CONFIG_NET_RANDOM_ETHADDR=y
+CONFIG_NET_FORCE_IPADDR=y
 CONFIG_USE_IPADDR=y
 CONFIG_IPADDR="192.168.1.1"
 CONFIG_USE_NETMASK=y
--- a/uboot-mtk-20250711/cmd/glbtn.c
+++ b/uboot-mtk-20250711/cmd/glbtn.c
@@ -49,8 +49,14 @@
 static int do_glbtn(struct cmd_tbl *cmdtp, int flag, int argc, char *const argv[])
 {
 	const char *button_label = NULL;
-	int ret, counter = 0;
-	struct udevice *dev;
+	char buttons[128];
+	char *name;
+	struct udevice *dev[2];
+	int btn_count = 0;
+	int ret;
+	int counter = 0;
+	int i;
+	int pressed;
 	ulong ts;

 	led_control("ledblink", "blink_led", "250");
@@ -58,34 +64,70 @@
 	gpio_power_clr();

 	button_label = env_get("glbtn_key");
 	if (!button_label)
-		button_label = "reset";
+		button_label = "reset,mesh";
+
+	strncpy(buttons, button_label, sizeof(buttons) - 1);
+	buttons[sizeof(buttons) - 1] = '\0';
+
+	name = strtok(buttons, ",");
+
+	while (name && btn_count < 2) {
+
+		ret = button_get_by_label(name, &dev[btn_count]);
+
+		if (!ret)
+			btn_count++;
+
+		name = strtok(NULL, ",");
+	}

-	ret = button_get_by_label(button_label, &dev);
-	if (ret) {
-		printf("Button '%s' not found (err=%d)\n", button_label, ret);
+	if (!btn_count) {
+		printf("Buttons '%s' not found\n", button_label);
 		return CMD_RET_FAILURE;
 	}

-	if (!button_get_state(dev)) {
+	pressed = 0;
+
+	for (i = 0; i < btn_count; i++) {
+		if (button_get_state(dev[i])) {
+			pressed = 1;
+			break;
+		}
+	}
+
+	if (!pressed) {
 		poller_async_register(&led_p, "led_pa");
 		poller_call_async(&led_p, 1000000, led_action_post, NULL);
 		return CMD_RET_SUCCESS;
 	}

 	led_control("ledblink", "blink_led", "500");

-	printf("RESET button is pressed for: %2d second(s)", counter++);
+	printf("BUTTON is pressed for: %2d second(s)", counter++);

 	ts = get_timer(0);

-	while (button_get_state(dev) && counter < 4) {
+	while (counter < 4) {
+
+		pressed = 0;
+
+		for (i = 0; i < btn_count; i++) {
+			if (button_get_state(dev[i])) {
+				pressed = 1;
+				break;
+			}
+		}
+
+		if (!pressed)
+			break;
+
 		if (get_timer(ts) < 1000)
 			continue;

 		ts = get_timer(0);

 		printf("\b\b\b\b\b\b\b\b\b\b\b\b%2d second(s)", counter++);
 	}

 	printf("\n");
